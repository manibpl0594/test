node {
   properties([parameters([choice(choices: ['service1', 'service2','service3','service4','service5','service6',
   'service7','service8','service9','service10','service11','service12','service13','service14','service15',
   'service16','service17','service18','service19','service20','service21','service22','service23','service24','service25',
   'service26','service27','service28','service29','service30'], description: '', name: 'Choises')])])
   checkout scm 
     docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') 
   if (Choises.equals("service1")){
        def dockerfile = '/var/lib/jenkins/workspace/docker1/service1/Dockerfile .'
        def customImage = docker.build("manibpl0509/trivy-v2:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
    }
   else if (Choises.equals("service2")){
        def dockerfile = '/var/lib/jenkins/workspace/docker1/service2/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
   else if (Choises.equals("service3")){
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') {
        def dockerfile = '/var/lib/jenkins/workspace/docker/service3/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
   }
   else if (Choises.equals("service4")){
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') {
        def dockerfile = '/var/lib/jenkins/workspace/docker/service4/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
   }
   else if (Choises.equals("service5")){
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') {
        def dockerfile = '/var/lib/jenkins/workspace/docker/service5/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
   }
   else if (Choises.equals("service6")){
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') {
        def dockerfile = '/var/lib/jenkins/workspace/docker/service6/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
   }
   else if (Choises.equals("service7")){
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') {
        def dockerfile = '/var/lib/jenkins/workspace/docker/service7/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
   }
   else if (Choises.equals("service8")){
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') {
        def dockerfile = '/var/lib/jenkins/workspace/docker/service8/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
   }
   else if (Choises.equals("service9")){
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') {
        def dockerfile = '/var/lib/jenkins/workspace/docker/service9/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
   }
   else if (Choises.equals("service10")){
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') {
        def dockerfile = '/var/lib/jenkins/workspace/docker/service10/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
   }
   else if (Choises.equals("service11")){
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') {
        def dockerfile = '/var/lib/jenkins/workspace/docker/service11/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
   }
   else if (Choises.equals("service12")){
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') {
        def dockerfile = '/var/lib/jenkins/workspace/docker/service12/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
   }
   else if (Choises.equals("service13")){
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') {
        def dockerfile = '/var/lib/jenkins/workspace/docker/service12/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
   }
   else if (Choises.equals("service14")){
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') {
        def dockerfile = '/var/lib/jenkins/workspace/docker/service14/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
   }
   else if (Choises.equals("service15")){
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') {
        def dockerfile = '/var/lib/jenkins/workspace/docker/service15/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
   }
   else if (Choises.equals("service16")){
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') {
        def dockerfile = '/var/lib/jenkins/workspace/docker/service16/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
   }
   else if (Choises.equals("service17")){
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') {
        def dockerfile = '/var/lib/jenkins/workspace/docker/service17/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
   }
   else if (Choises.equals("service18")){
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') {
        def dockerfile = '/var/lib/jenkins/workspace/docker/service18/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
   }
   else if (Choises.equals("service19")){
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') {
        def dockerfile = '/var/lib/jenkins/workspace/docker/service19/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
   }
   else if (Choises.equals("service20")){
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') {
        def dockerfile = '/var/lib/jenkins/workspace/docker/service20/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
   }
   else if (Choises.equals("service21")){
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') {
        def dockerfile = '/var/lib/jenkins/workspace/docker/service21/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
   }
   else if (Choises.equals("service22")){
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') {
        def dockerfile = '/var/lib/jenkins/workspace/docker/service22/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
   }
   else if (Choises.equals("service23")){
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') {
        def dockerfile = '/var/lib/jenkins/workspace/docker/service23/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
   }
   else if (Choises.equals("service24")){
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') {
        def dockerfile = '/var/lib/jenkins/workspace/docker/service24/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
   }
   else if (Choises.equals("service25")){
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') {
        def dockerfile = '/var/lib/jenkins/workspace/docker/service25/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
   }
   else if (Choises.equals("service26")){
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') {
        def dockerfile = '/var/lib/jenkins/workspace/docker/service26/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
   }
   else if (Choises.equals("service27")){
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') {
        def dockerfile = '/var/lib/jenkins/workspace/docker/service27/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
   }
   else if (Choises.equals("service28")){
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') {
        def dockerfile = '/var/lib/jenkins/workspace/docker/service28/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
   }
   else if (Choises.equals("service29")){
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') {
        def dockerfile = '/var/lib/jenkins/workspace/docker/service29/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
   }
   else if (Choises.equals("service30")){
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com', 'Dockerhub_id') {
        def dockerfile = '/var/lib/jenkins/workspace/docker/service30/Dockerfile .'
        def customImage = docker.build("manibpl0509/golang-app:${env.BUILD_ID}", "-f ${dockerfile}")

        /* Push the container to the custom Registry */
        customImage.push()
   }
}
}
